{
  "name": "guilda-peaky",
  "version": "1.0.0",
  "description": "Sistema de Gerenciamento de Ouro - Peaky Blinders Guild",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "setup": "cd server && npx prisma generate && npx prisma migrate dev --name init && node src/utils/seed.js",
    "build": "cd client && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
